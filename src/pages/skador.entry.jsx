import React from 'react';
import ReactDOM from 'react-dom/client';
import { SkadorPage } from './SkadorPage';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SkadorPage />
  </React.StrictMode>
);
